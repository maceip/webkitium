# Linux Chrome

Linux is not the priority of this chrome pass, but it should use the same shared adapter contract as the other native shells.

Likely stack if product scope expands: GTK4/libadwaita with WPEGTK or the project engine surface. Keep Linux-specific WebAuthn work in browser core and keep UI decisions native to the desktop environment.
