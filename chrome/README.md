# Per-platform chrome

Native shell / chrome around the engine: **one subdirectory per OS**. Source and resources for that platform’s UI and glue—not patches to the upstream WebKit tree (those stay under `webkit/patches/`).
