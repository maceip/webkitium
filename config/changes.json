{
  "activeChanges": [
    {
      "id": "passkeys-credentials-get",
      "enabled": false,
      "platforms": ["android", "windows", "macos", "linux", "ios"]
    },
    {
      "id": "windows-webgpu-service",
      "enabled": true,
      "platforms": ["windows"]
    },
    {
      "id": "windows-minibrowser-fluent-tabs",
      "enabled": false,
      "platforms": ["windows"]
    },
    {
      "id": "webnn-service",
      "enabled": true,
      "platforms": ["windows"]
    }
  ],
  "_comment": "Lane ids are stable names for changes/<id>/. WebGPU patches live under webkit/patches/windows/ and are always applied. webnn-service is the WebNN (navigator.ml) integration lane using LiteRT-LM as the unified C++ backend."
}
