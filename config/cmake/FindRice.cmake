# Stub: librice (Rust ICE library) is not packaged for Ubuntu.
# Overrides WebKit's Source/cmake/FindRice.cmake so configure completes.
# USE_LIBRICE=OFF prevents any usage.
set(Rice_FOUND FALSE)
set(RICE_FOUND FALSE)
