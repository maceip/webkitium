# Stub: Thunder (WPE/RDK framework) is not installed on this runner.
# OpenCDM headers + stub libocdm.so are installed separately.
# Report as found so the CDM code links against the stub library.
set(THUNDER_FOUND TRUE)
set(Thunder_FOUND TRUE)
set(THUNDER_INCLUDE_DIR "/usr/include")
set(THUNDER_LIBRARIES "ocdm")
set(THUNDER_LIBRARY "ocdm")
