# Build runner / orchestrator

HTTP (or similar) service that **starts builds**, assigns build ids, tracks running jobs, streams logs, checkpoints, and artifact metadata.

Implementation language and framework TBD; this directory holds that service only—not WebKit patches or browser source.
