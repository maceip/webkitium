diff --git a/Source/WebCore/Modules/WebNN/ML.cpp b/Source/WebCore/Modules/WebNN/ML.cpp
new file mode 100644
index 0000000000..18a6ce387c
--- /dev/null
+++ b/Source/WebCore/Modules/WebNN/ML.cpp
@@ -0,0 +1,60 @@
+/*
+ * Copyright (C) 2024-2026 Webkitium Contributors
+ */
+
+#include "config.h"
+#include "Modules/WebNN/ML.h"
+
+#if ENABLE(WEBNN)
+
+#include "Modules/WebNN/MLContext.h"
+#include "Modules/WebNN/MLContextOptions.h"
+#include "Modules/WebNN/Implementation/LiteRTLoader.h"
+#include "Modules/WebNN/Implementation/WebNNLiteRTContext.h"
+#include "bindings/js/JSDOMConvertInterface.h"
+#include "bindings/js/JSDOMPromiseDeferred.h"
+#include <wtf/StdLibExtras.h>
+
+namespace WebCore {
+
+Ref<ML> ML::create(Navigator& navigator)
+{
+    UNUSED_PARAM(navigator);
+    return adoptRef(*new ML(navigator));
+}
+
+ML::ML(Navigator&)
+{
+}
+
+ML::~ML() = default;
+
+void ML::createContext(MLContextOptions&& options, Ref<DeferredPromise>&& promise)
+{
+    auto& loader = WebNN::LiteRTLoader::singleton();
+    if (!loader.isLoaded()) {
+        promise->reject(Exception { ExceptionCode::NotSupportedError,
+            "WebNN: LiteRT runtime library not available"_s });
+        return;
+    }
+
+    auto backendContext = WebNN::LiteRTContext::create(options);
+    if (!backendContext) {
+        promise->reject(Exception { ExceptionCode::NotSupportedError,
+            "WebNN: failed to create ML context"_s });
+        return;
+    }
+
+    auto mlContext = MLContext::create(WTF::move(backendContext));
+    if (!mlContext) {
+        promise->reject(Exception { ExceptionCode::NotSupportedError,
+            "WebNN: failed to wrap ML context"_s });
+        return;
+    }
+
+    promise->resolve<IDLInterface<MLContext>>(mlContext.releaseNonNull());
+}
+
+} // namespace WebCore
+
+#endif // ENABLE(WEBNN)
diff --git a/Source/WebCore/Modules/WebNN/ML.h b/Source/WebCore/Modules/WebNN/ML.h
new file mode 100644
index 0000000000..cd78645c83
--- /dev/null
+++ b/Source/WebCore/Modules/WebNN/ML.h
@@ -0,0 +1,34 @@
+/*
+ * Copyright (C) 2024-2026 Webkitium Contributors
+ *
+ * https://webmachinelearning.github.io/webnn/#api-ml
+ */
+
+#pragma once
+
+#if ENABLE(WEBNN)
+
+#include <wtf/RefCounted.h>
+#include <wtf/RefPtr.h>
+
+namespace WebCore {
+
+class MLContext;
+struct MLContextOptions;
+class DeferredPromise;
+class Navigator;
+
+class ML final : public RefCounted<ML> {
+public:
+    static Ref<ML> create(Navigator&);
+    ~ML();
+
+    void createContext(MLContextOptions&&, Ref<DeferredPromise>&&);
+
+private:
+    explicit ML(Navigator&);
+};
+
+} // namespace WebCore
+
+#endif // ENABLE(WEBNN)
diff --git a/Source/WebCore/Modules/WebNN/ML.idl b/Source/WebCore/Modules/WebNN/ML.idl
new file mode 100644
index 0000000000..5b512f4cdd
--- /dev/null
+++ b/Source/WebCore/Modules/WebNN/ML.idl
@@ -0,0 +1,13 @@
+/*
+ * https://webmachinelearning.github.io/webnn/#api-ml
+ */
+
+[
+    Conditional=WEBNN,
+    Exposed=Window,
+    SecureContext
+] interface ML {
+    [NewObject] Promise<MLContext> createContext(optional MLContextOptions options = {});
+
+    // Future: createContext(GPUDevice gpuDevice) for WebGPU interop
+};
diff --git a/Source/WebCore/Modules/WebNN/NavigatorML.cpp b/Source/WebCore/Modules/WebNN/NavigatorML.cpp
new file mode 100644
index 0000000000..f29d454af0
--- /dev/null
+++ b/Source/WebCore/Modules/WebNN/NavigatorML.cpp
@@ -0,0 +1,57 @@
+/*
+ * Copyright (C) 2024-2026 Webkitium Contributors
+ */
+
+#include "config.h"
+#include "Modules/WebNN/NavigatorML.h"
+
+#if ENABLE(WEBNN)
+
+#include "Modules/WebNN/ML.h"
+#include "page/Navigator.h"
+#include <wtf/TZoneMallocInlines.h>
+#include <wtf/StdLibExtras.h>
+
+namespace WebCore {
+
+WTF_MAKE_TZONE_ALLOCATED_IMPL(NavigatorML);
+
+NavigatorML::NavigatorML(Navigator& navigator)
+    : m_navigator(navigator)
+{
+}
+
+NavigatorML::~NavigatorML() = default;
+
+ASCIILiteral NavigatorML::supplementName()
+{
+    return "NavigatorML"_s;
+}
+
+NavigatorML* NavigatorML::from(Navigator& navigator)
+{
+    auto* supplement = static_cast<NavigatorML*>(
+        Supplement<Navigator>::from(&navigator, supplementName()));
+    if (!supplement) {
+        auto newSupplement = makeUnique<NavigatorML>(navigator);
+        supplement = newSupplement.get();
+        provideTo(&navigator, supplementName(), WTF::move(newSupplement));
+    }
+    return supplement;
+}
+
+ML* NavigatorML::ml(Navigator& navigator)
+{
+    return NavigatorML::from(navigator)->ml();
+}
+
+ML* NavigatorML::ml()
+{
+    if (!m_ml)
+        m_ml = ML::create(m_navigator);
+    return m_ml.get();
+}
+
+} // namespace WebCore
+
+#endif // ENABLE(WEBNN)
diff --git a/Source/WebCore/Modules/WebNN/NavigatorML.h b/Source/WebCore/Modules/WebNN/NavigatorML.h
new file mode 100644
index 0000000000..cb289332e8
--- /dev/null
+++ b/Source/WebCore/Modules/WebNN/NavigatorML.h
@@ -0,0 +1,37 @@
+/*
+ * Copyright (C) 2024-2026 Webkitium Contributors
+ */
+
+#pragma once
+
+#if ENABLE(WEBNN)
+
+#include "page/Navigator.h"
+#include "platform/Supplementable.h"
+#include <wtf/TZoneMalloc.h>
+#include <wtf/RefPtr.h>
+
+namespace WebCore {
+
+class ML;
+
+class NavigatorML : public Supplement<Navigator> {
+    WTF_MAKE_TZONE_ALLOCATED(NavigatorML);
+public:
+    explicit NavigatorML(Navigator&);
+    virtual ~NavigatorML();
+
+    static NavigatorML* from(Navigator&);
+    static ML* ml(Navigator&);
+    ML* ml();
+
+private:
+    static ASCIILiteral supplementName();
+
+    RefPtr<ML> m_ml;
+    Navigator& m_navigator;
+};
+
+} // namespace WebCore
+
+#endif // ENABLE(WEBNN)
diff --git a/Source/WebCore/Modules/WebNN/NavigatorML.idl b/Source/WebCore/Modules/WebNN/NavigatorML.idl
new file mode 100644
index 0000000000..c4999d288f
--- /dev/null
+++ b/Source/WebCore/Modules/WebNN/NavigatorML.idl
@@ -0,0 +1,10 @@
+/*
+ * https://webmachinelearning.github.io/webnn/#api-navigator-ml
+ */
+
+[
+    Conditional=WEBNN,
+    Exposed=Window
+] partial interface Navigator {
+    [SecureContext] readonly attribute ML ml;
+};
